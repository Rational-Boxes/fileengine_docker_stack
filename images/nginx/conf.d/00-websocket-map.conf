# Maps the Upgrade header to the Connection header for WebSocket proxying
# (used by the /csai/chat passthrough). http-context; loaded before the vhosts.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
